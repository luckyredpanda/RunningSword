<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="22dp"
    android:layout_marginTop="8dp"
    android:layout_marginEnd="32dp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/game_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp"
        android:text="New game"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/game_participants"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:drawableLeft="@drawable/ic_users"
        android:drawablePadding="8dp"
        app:drawableTint="@color/fontBackground"
        android:text="1 Participant" />

    <TextView
        android:id="@+id/game_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:drawableLeft="@drawable/ic_calendar"
        android:drawablePadding="8dp"
        app:drawableTint="@color/fontBackground"
        android:text="0 days left" />

    <TextView
        android:id="@+id/game_distance"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:layout_marginTop="4dp"
        android:drawableLeft="@drawable/ic_flag"
        android:drawablePadding="11dp"
        android:text="0 km to go"
        app:drawableTint="@color/fontBackground" />
</LinearLayout>
